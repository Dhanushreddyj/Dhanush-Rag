# Enterprise AI Platform — direct runtime dependencies

#

# Python support: CPython 3.14.x only (>=3.14,<3.15)

#

# This is the target runtime dependency set for the approved architecture.

# Generate a fully pinned deployment artifact from this file during release

# (for example with pip-tools or uv). Do not add ChromaDB or provider-specific

# LangChain integrations here.

# API and configuration

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0

# Agent orchestration

# LangGraph owns orchestration only; application business logic remains

# independent of LangGraph internals.

langgraph>=1.0,<2.0

# Approved external-system adapters

# Qdrant Cloud and self-hosted Qdrant are accessed through this client; no

# Qdrant server package belongs in the Python dependency set.

qdrant-client>=1.18,<2.0
boto3>=1.40,<2.0

# OpenAI-compatible protocol client for the LM Studio development adapter.

# This does not select OpenAI as the production provider.

openai>=1.54,<2.0

# Ingestion and document processing

pypdf>=4.3,<7.0
docx2txt>=0.8,<1.0

# Async HTTP, retries, and Server-Sent Events

httpx>=0.27,<1.0
tenacity>=9.0,<10.0
sse-starlette>=3.4,<4.0
